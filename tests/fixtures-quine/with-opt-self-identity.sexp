; This fixture is a placeholder. The actual :opt-self value must be
; the current *self-source* of the quine at test time.
; harness-quine.sh extracts *self-source* from the baseline and
; constructs the full plist inline.
(:user "Hello" :thinking "Testing opt-self identity" :assistant "OK" :current-focus "" :theory-of-mind "" :self-narrative "" :annotation "")
